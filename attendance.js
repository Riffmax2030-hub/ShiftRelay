async function addAttendancePanel(){if(!state.user||!['owner','workforce_admin'].includes(state.user.role)||$('#page-title').textContent!=='Analytics'||$('#attendance-panel'))return;try{const data=await api('/api/attendance/today');const rows=data.attendance.map((worker)=>'<article class="handover-item"><div><h3>'+escape(worker.name)+'</h3><p>'+escape(worker.title)+' · '+worker.hours_today+'h today</p></div><span class="badge '+(worker.status==='clocked_in'?'acknowledged':worker.status==='clocked_out'?'relayed':'awaiting_review')+'">'+escape(worker.status.replace('_',' '))+'</span></article>').join('')||'<p class="empty">No active workers yet.</p>';const panel=document.createElement('section');panel.className='workspace-card';panel.id='attendance-panel';panel.innerHTML='<p class="eyebrow">TODAY’S ATTENDANCE</p><h2>Who is on shift right now?</h2><div class="action-list">'+rows+'</div>';$('#dashboard').append(panel)}catch(error){console.warn('Attendance unavailable',error)}}
new MutationObserver(()=>addAttendancePanel()).observe($('#dashboard'),{childList:true,subtree:true});
